horizons: [21, 63, 126]

volatility:
  enabled: true
  annualize: true
  buckets: {method: train_quantile, n_bins: 5}

trend:
  enabled: true
  normalize_by_future_vol: true
  buckets: {method: train_quantile, n_bins: 5}

drawdown:
  enabled: true
  buckets: {method: train_quantile, n_bins: 5}

dispersion:
  enabled: true
  universe: optional_equity_universe
  buckets: {method: train_quantile, n_bins: 5}

regime_labels:
  enabled: true
  labels: [crash, rebound, trend, chop, calm_up]

